global:
  scrape_interval: 5s

scrape_configs:
  - job_name: 'churn_api'
    static_configs:
      # Use host.docker.internal to scrape the API running on the host machine
      - targets: ['host.docker.internal:8000']
